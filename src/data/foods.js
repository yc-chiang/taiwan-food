/**
 * 台灣經典食物清單 —— 尚待填入。
 *
 * 你提供清單後，把資料放進這個陣列即可，其他程式碼都不用改。
 * 格式與各維度的意義請見 docs/INTEGRATION.md 的「食物資料格式」。
 *
 * 快速產生空白範本：
 *   import { blankProfileTemplate } from '../match/schema.js';
 *   console.log(blankProfileTemplate());
 *
 * 範例（僅供格式參考，非最終資料）：
 *   {
 *     id: 'stinky-tofu',
 *     name: '臭豆腐',
 *     description: '滾油裡滋滋作響',
 *     profile: { roughness: 0.9, brightness: 0.35, rhythm: 0.7, tonality: 0.1 },
 *   }
 */
export const foods = [];

export default foods;
